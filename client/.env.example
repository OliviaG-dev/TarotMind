# Même identifiant client que côté API (GOOGLE_CLIENT_ID) — pour le bouton Google sur le front.
VITE_GOOGLE_CLIENT_ID=xxxx.apps.googleusercontent.com

# Optionnel : URL de base de l’API (défaut : chemins relatifs /api via proxy Vite en dev).
# VITE_API_BASE=/api
